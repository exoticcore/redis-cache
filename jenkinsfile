pipeline {
    agent any

    environment {
        DEV_HOST = "34.87.155.25"
        SSH_CREDENTIALS_ID = "ssh-dev-credentials-id"
    }

    stages {
        stage('Deploy') {
            steps {
                withCredentials([sshUserPrivateKey(credentialsId: SSH_CREDENTIALS_ID, keyFileVariable: 'SSH_KEY', usernameVariable: 'SSH_USER')]) {
                        scp -o StrictHostKeyChecking=no -i ${SSH_KEY} docker-compose.yaml ${SSH_USER}@34.87.155.25:/home/${SSH_USER}/
Shell Script
0.3 sec
Warning: A secret was passed to "sh" using Groovy String interpolation, which is insecure.
		 Affected argument(s) used the following variable(s): [SSH_KEY, SSH_USER]
		 See https://jenkins.io/redirect/groovy-string-interpolation for details.
/var/lib/jenkins/workspace/Deployment(Dev)@tmp/durable-ff3b6ea3/script.sh.copy: 1: Syntax error: "(" unexpected
script returned exit code 2
                    }
            }
        }
    }
}