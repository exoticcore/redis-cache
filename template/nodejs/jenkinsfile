pipeline {
    environment {
        registry = ""
        registryCredential = ""
        dockerImage = ''
    }
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World from Main'
            }
        }
        stage('list files') {
            steps {
                sh 'ls -la'
            }
        }
        // stage('Build Docker Image') {
        //     steps {
        //         dir('template/nodejs') {
        //             dockerImage = docker.build("${registry}:$BUILD_NUMBER")
        //         }
        //     }
        // }
    }
}